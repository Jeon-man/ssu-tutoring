---
title: 'Pep/8 어셈블리 — 입력값 조건 분기 연습'
publishedAt: '2026-06-05'
summary: '컴퓨터 시스템: Pep/8 어셈블리로 입력값을 비교(CPA)하고 분기(BRLT)하여 메시지를 출력하는 두 가지 프로그램'
subject: 'computersystems'
---

---

## 사용하는 명령어·지시어 정리

<table>
  <thead>
    <tr>
      <th scope="col">구분</th>
      <th scope="col">표기</th>
      <th scope="col">의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>입력</td>
      <td>
        <code>DECI 대상,d</code>
      </td>
      <td>10진수를 입력받아 메모리(대상)에 저장</td>
    </tr>
    <tr>
      <td>적재</td>
      <td>
        <code>LDA 대상,d</code>
      </td>
      <td>메모리 값을 누산기(A)로 불러오기</td>
    </tr>
    <tr>
      <td>덧셈</td>
      <td>
        <code>ADDA 대상,d</code>
      </td>
      <td>A에 메모리 값을 더하기</td>
    </tr>
    <tr>
      <td>비교</td>
      <td>
        <code>CPA 값,i</code>
      </td>
      <td>
        A에서 값을 뺀 결과로 <strong>상태비트(N,Z,V,C)만</strong> 설정 (A 값은
        변하지 않음). 뺄셈 결과의 부호로 크기를 비교할 때 사용
      </td>
    </tr>
    <tr>
      <td>분기</td>
      <td>
        <code>BRLT 라벨</code>
      </td>
      <td>
        직전 비교 결과가 <strong>작으면(less than)</strong> 라벨로 점프
      </td>
    </tr>
    <tr>
      <td>분기</td>
      <td>
        <code>BR 라벨</code>
      </td>
      <td>무조건 라벨로 점프</td>
    </tr>
    <tr>
      <td>출력</td>
      <td>
        <code>STRO 대상,d</code>
      </td>
      <td>
        대상 주소부터 <strong>널(\x00)</strong>을 만날 때까지 문자열 출력
      </td>
    </tr>
    <tr>
      <td>지시어</td>
      <td>
        <code>.BLOCK n</code>
      </td>
      <td>n바이트 메모리 공간 확보 (변수 자리)</td>
    </tr>
    <tr>
      <td>지시어</td>
      <td>
        <code>.ASCII "..."</code>
      </td>
      <td>
        문자열 상수 저장 (출력용으로 끝에 <code>\x00</code> 필요)
      </td>
    </tr>
    <tr>
      <td>종료</td>
      <td>
        <code>STOP</code> / <code>.END</code>
      </td>
      <td>프로그램 실행 종료 / 소스 끝 표시</td>
    </tr>
  </tbody>
</table>

> **참고.** `i`는 즉시(immediate) 주소 지정으로 피연산자 **자체가 값**, `d`는 직접(direct) 주소 지정으로 피연산자가 **메모리 주소**를 뜻합니다. 정수형 변수는 2바이트(워드)이므로 `.BLOCK 2`로 잡습니다.

---

## if 문을 어셈블리로 옮길 때 — "거짓일 때 먼저 분기한다"

고급 언어의 `if (조건) { 참동작 } else { 거짓동작 }`을 어셈블리로 옮길 때는, **조건이 거짓일 때 곧바로 분기**해서 참동작을 건너뛰도록 작성하는 것이 정석입니다.

```text
if (num >= 100) {        ; 우리가 원하는 조건(참)
    참동작;
} else {
    거짓동작;
}
```

어셈블리에는 "조건이 참이면 실행"이 따로 없고 **조건 분기 명령(`BRLT` 등)** 뿐입니다. 그래서 위 조건을 그대로 옮기는 대신, **부정(거짓) 조건**으로 바꿔서 분기합니다.

- 원하는 조건: `num >= 100` (참이면 "100 이상" 출력)
- 분기에 쓰는 조건: 그 **반대인 `num < 100`** → `BRLT`로 거짓 처리(else)로 점프

즉 코드 흐름은 이렇게 됩니다.

```text
        CPA   100,i      ; 비교
        BRLT  less       ; (거짓) num < 100 이면 else로 먼저 분기
        ... 참동작 ...     ; 분기하지 않았다면 조건이 참 → 참동작 실행
        BR    done       ; 참동작 후 else를 건너뛰어 종료로
less:   ... 거짓동작 ...   ; 거짓일 때 실행
done:
```

**핵심:** 분기 명령은 **거짓(원하는 조건의 반대)** 을 먼저 검사해 점프시키고, 분기하지 않고 그대로 흘러내려간 경우가 "조건이 참"인 상황이 됩니다. 참동작 끝에는 반드시 `BR`로 거짓동작(else)을 건너뛰어야 둘 다 실행되는 실수를 막을 수 있습니다.

<Image
  src="/cs/if_state.webp"
  alt="if 문을 어셈블리 조건 분기로 변환하는 흐름"
  width={972}
  height={554}
/>

---

## 문제 1 — 숫자 1개와 0 비교

**요구사항**

- 숫자 1개를 입력받습니다.
- 그 값이 **0 이상**이면 `greater than or equal to 0`을 출력합니다.
- 그렇지 않으면 `less than 0`을 출력합니다.

**설계 흐름**

1. `DECI`로 숫자를 메모리 `num`에 입력받는다.
2. `LDA`로 `num`을 A에 불러온다.
3. `num < 0`이면 `BRLT`로 "less" 라벨로 분기, 아니면 그대로 진행하여 "0 이상" 메시지를 출력한다.

<RevealAnswer title="문제 1 정답 보기 (클릭하여 펼침)">

```asm
         BR      main        ; 데이터 영역을 건너뛰고 main으로 점프
num:     .BLOCK  2           ; 입력 숫자를 저장할 변수 (2바이트)
great:   .ASCII  "greater than or equal to 0\x00"
less:   .ASCII  "less than 0\x00"

main:    DECI    num,d       ; 숫자 1개를 입력받아 num에 저장
         LDA     num,d       ; num 값을 누산기 A로 적재
         BRLT    else        ; num < 0 이면 less 라벨로 분기
         STRO    great,d     ; (0 이상) "greater than or equal to 0" 출력
         BR      endIf        ; 출력 끝났으니 종료 지점으로 점프
else:    STRO    less,d     ; (0 미만) "less than 0" 출력

endIf:    STOP                ; 프로그램 종료
         .END
```

`LDA`는 값을 적재하면서 **상태비트(N 등)** 도 함께 설정합니다. 0과의 비교는 곧 **부호 판별**이라, 별도 비교 없이 `LDA` 직후 `BRLT`로 바로 분기할 수 있습니다.

</RevealAnswer>

---

## 문제 2 — 숫자 1개와 100 비교

**요구사항**

- 숫자 1개를 입력받습니다.
- 그 값이 **100 이상**이면 `greater than or equal to 100`을 출력합니다.
- 그렇지 않으면 `less than 100`을 출력합니다.

**설계 흐름**

문제 1과 구조가 같고, 비교 대상만 0에서 **100**으로 바뀝니다. `NEGA`로 음수를 만들고 `ADDA 100,i`로 100을 더해 100 이상인지 판별합니다.

<RevealAnswer title="문제 2 정답 보기 — CPA 없이 (클릭하여 펼침)">

```asm
         BR      main        ; 데이터 영역을 건너뛰고 main으로 점프
num:     .BLOCK  2           ; 입력 숫자를 저장할 변수 (2바이트)
great:   .ASCII  "greater than or equal to 100\x00"
less:   .ASCII  "less than 100\x00"

main:    DECI    num,d       ; 숫자 1개를 입력받아 num에 저장
         LDA     num,d       ; num 값을 누산기 A로 적재
         NEGA                 ; num = -num
         ADDA    0x0064,i     ; num = num + 100
         BRLT    else        ; num < 0 이면 less 라벨로 분기
         STRO    great,d     ; (100 이상) "greater than or equal to 100" 출력
         BR      endIf        ; 출력 끝났으니 종료 지점으로 점프

else:    STRO    less,d     ; (100 미만) "less than 100" 출력

endIf:    STOP                ; 프로그램 종료
         .END
```

비교 대상 값만 바꾸면 되고, 나머지 분기·출력 구조는 문제 1과 동일합니다.

</RevealAnswer>

<RevealAnswer title="문제 2 정답 보기 — CPA 버전 (클릭하여 펼침)">

```asm
         BR      main        ; 데이터 영역을 건너뛰고 main으로 점프
num:     .BLOCK  2           ; 입력 숫자를 저장할 변수 (2바이트)
great:   .ASCII  "greater than or equal to 100\x00"
less:    .ASCII  "less than 100\x00"

main:    DECI    num,d       ; 숫자 1개를 입력받아 num에 저장
         LDA     num,d       ; A = num
         CPA     100,i       ; A - 100 으로 상태비트만 설정 (A는 그대로)
         BRLT    else        ; num < 100 이면 else로 분기

         STRO    great,d     ; (100 이상) "greater than or equal to 100" 출력
         BR      endIf       ; else를 건너뛰어 종료로

else:    STRO    less,d      ; (100 미만) "less than 100" 출력

endIf:   STOP                ; 프로그램 종료
         .END
```

`CPA`는 사실상 **`SUBA`가 결과를 버리는 버전**입니다. 위의 무(無)CPA 버전과 달리 A 값을 보존하므로, 비교 후에도 `num` 원본 값을 계속 써야 할 때 유용합니다.

</RevealAnswer>

---

## 문제 3 — 숫자 2개의 합과 0 비교

**요구사항**

- 숫자 2개를 입력받습니다.
- 두 수의 **합이 0 이상**이면 `greater than or equal to 0`을 출력합니다.
- 그렇지 않으면 `less than 0`을 출력합니다.

**설계 흐름**

1. `DECI`를 두 번 사용해 `num1`, `num2`를 각각 입력받는다.
2. `LDA num1,d`로 첫 수를 A에 넣고 `ADDA num2,d`로 둘째 수를 더해 **합**을 만든다.
3. 합과 0을 비교해 음수면 `BRLT`로 "else" 라벨로 분기한다. (0과 비교이므로 `CPA`는 생략 가능)

<RevealAnswer title="문제 3 정답 보기 — CPA 없이 (클릭하여 펼침)">

```asm
         BR      main        ; 데이터 영역을 건너뛰고 main으로 점프
num1:    .BLOCK  2           ; 첫 번째 입력 숫자
num2:    .BLOCK  2           ; 두 번째 입력 숫자
great:   .ASCII  "greater than or equal to 0\x00"
less:    .ASCII  "less than 0\x00"

main:    DECI    num1,d      ; 첫 번째 숫자 입력
         DECI    num2,d      ; 두 번째 숫자 입력

         LDA     num1,d      ; A = num1
         ADDA    num2,d      ; A = num1 + num2 (합, 상태비트도 설정됨)
         BRLT    else        ; 합 < 0 이면 else로 분기

         STRO    great,d     ; (0 이상) "greater than or equal to 0" 출력
         BR      endIf       ; else를 건너뛰어 종료로

else:    STRO    less,d      ; (0 미만) "less than 0" 출력

endIf:   STOP                ; 프로그램 종료
         .END
```

`ADDA`가 덧셈 결과에 맞춰 상태비트(N 등)를 설정하므로, 0과의 비교는 별도 `CPA` 없이 `BRLT`로 바로 분기할 수 있습니다.

</RevealAnswer>
