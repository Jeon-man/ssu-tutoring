---
title: "데이터 표현: 실수의 표현과 부동 소수점 (1주차)"
publishedAt: "2026-04-09"
summary: "컴퓨터 시스템 1주차: 컴퓨터의 실수 표현 방식, 부동 소수점의 구조와 오차 발생 원리 학습"
subject: "computersystems"
---

이번 주차는 **실수가 메모리에 어떻게 저장되는지**, 특히 **부동 소수점의 구조와 한계**를 이해하는 데 초점을 둡니다.

---

## 1. 실수의 개요와 C 언어의 자료형

컴퓨터 안에서 실수는 정수와 **원리가 다릅니다.** C에서 많이 쓰는 실수형은 아래와 같습니다.

<table>
  <thead>
    <tr>
      <th scope="col">타입</th>
      <th scope="col">크기</th>
      <th scope="col">대략적인 범위 (10진)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>float</strong></td>
      <td>4바이트</td>
      <td>단정밀도. 대략 최대 절댓값 ±3.4×10³⁸ 근처, 최소 양의 정규화 수는 약 1.2×10⁻³⁸ 부근(표준 근사)</td>
    </tr>
    <tr>
      <td><strong>double</strong></td>
      <td>8바이트</td>
      <td>배정밀도. 대략 ±1.7×10^308 근처까지(표준 근사)</td>
    </tr>
  </tbody>
</table>

`double`은 메모리를 더 쓰지만 **범위가 넓고 정밀도가 높은** 편입니다.

---

## 2. 실수의 정밀도와 오차 문제

비트 수는 유한한데 실수는 무한에 가깝기 때문에 **근사값**을 쓰게 되고, 그 과정에서 **오차**가 생깁니다.

### 실습 예제: 실수 연산의 한계

```c
#include <stdio.h>

void float_test() {
    float a = 0.2;
    float b = 0.2;

    if (a + b == 0.4) {
        printf("정확함\n");
    } else {
        printf("오차 발생\n"); // 실제 실행되는 구간
    }
}
```

**왜 이런 일이 나는가**

- 상수 `0.4`는 보통 **`double`**으로 해석되고, `a + b`는 **`float`** 연산 결과입니다. **비트 수·해석 방식이 다른 두 값**을 `==`로 비교하면 미세한 차이 때문에 다르다고 나올 수 있습니다.

**실무에서의 습관**

- 정밀도가 중요하면 **반복 덧셈**보다 **한 번의 곱셈** 등으로 오차 누적을 줄이는 방향을 고려합니다.

---

## 3. 고정 소수점 vs 부동 소수점

| 방식 | 특징 |
| --- | --- |
| **고정 소수점** | 소수점 위치를 미리 고정 (예: 상위 8비트 정수부, 하위 8비트 소수부). 연산은 단순·빠를 수 있으나 **표현 범위가 좁음**. |
| **부동 소수점** | 소수점 위치가 **떠다님**. 과학적 표기법(a×10ᵇ)을 **2진**에 옮긴 형태에 가깝고, 지금은 대부분 **IEEE 754** 표준을 따릅니다. |

---

## 4. 부동 소수점의 구조 (IEEE 754)

한 실수를 크게 **세 덩어리**로 나눕니다.

1. **부호 (Sign)** — 양수 `0`, 음수 `1`
2. **지수 (Exponent)** — 전체 **스케일**(크기 차수)
3. **가수 (Mantissa / Significand)** — **유효 숫자** 쪽 정밀도

### 정규화와 Hidden Bit

실수를 **1.xxx × 2ⁿ** 꼴로 맞추는 과정이 **정규화**입니다. 이때 소수점 앞은 항상 **1**이라서, 메모리에는 안 넣고 생략하기도 하는데 이를 **Hidden Bit**라고 합니다. 가수 비트를 아끼면서 정밀도를 올리는 트릭입니다.

### 초과 표현 (Excess / Bias)

지수에 **음수**도 필요하지만, 지수만 2의 보수로 넣으면 **실수끼리 비교**가 번거로워질 수 있습니다. 그래서 **Bias를 더해 모두 양수처럼** 저장해 두고, 하드웨어에서는 **정수 비교처럼** 빠르게 순서를 매깁니다.

---

## 5. 특수 값 (Special Values)

<table>
  <thead>
    <tr>
      <th scope="col">이름</th>
      <th scope="col">의미</th>
    </tr>
  </thead>
  <tbody>
    <tr><td><strong>0</strong></td><td>비트가 전부 0에 가까운 형태의 영</td></tr>
    <tr><td><strong>Overflow</strong></td><td>표현 가능한 최대를 넘김</td></tr>
    <tr><td><strong>Underflow</strong></td><td>0에 너무 가까워 최소 표현 아래로 감</td></tr>
    <tr><td><strong>NaN</strong></td><td>0/0 같이 **정의되지 않은** 연산 결과</td></tr>
  </tbody>
</table>

---

## 연습 문제

> **문제.** 10진수 **6.585**를 아래 규격의 **10비트 부동 소수점**으로 변환하시오.
>
> - 부호: 1 bit  
> - 지수: 4 bit (**7-초과 표현**)  
> - 가수: 5 bit  
