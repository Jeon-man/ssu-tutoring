---
title: '프로그래밍1 4주차 — 코드 빈칸'
publishedAt: '2026-04-26'
summary: '프로그래밍1: for·while·조건·switch·중첩·문자열 — 기초부터 응용까지 빈칸 문제'
subject: 'programming1'
---

---

## 기초

### 문제 1 — **1부터 10까지** 출력 (`for`)

`for` 반복문으로 **`1`, `2`, …, `10`을 각 줄에** 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    for (int i = (          ); i <= (          ); i++) {
        cout << i << endl;
    }
    return 0;
}
```

**예:** 줄마다 `1`부터 `10`까지 한 줄에 하나씩.

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `1`
- 두 번째 빈칸 → `10`

```cpp
#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 10; i++) {
        cout << i << endl;
    }
    return 0;
}
```

</RevealAnswer>

---

### 문제 2 — **`1 ~ n` 합** (`for`)

양의 정수 `n`을 입력받아 **`1 + 2 + … + n`** 을 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int s = 0;
    for (int i = 1; (          ); i++) {
        s += (          );
    }
    cout << s << endl;
    return 0;
}
```

**예:** `n = 5` → 출력 `15` / `n = 1` → 출력 `1`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `i <= n`
- 두 번째 빈칸 → `i`

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int s = 0;
    for (int i = 1; i <= n; i++) {
        s += i;
    }
    cout << s << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 3 — **`1 ~ hi` 중 `k`의 배수만** 출력 (`for` + `%`)

`hi`(끝 수)와 `k`(나누어떨어짐을 검사할 값)를 입력받습니다. **`1`부터 `hi`까지** 중 **`k`의 배수**만 한 줄에 하나씩 출력합니다 (`hi >= 1`, `k >= 1` 가정).

```cpp
#include <iostream>
using namespace std;

int main() {
    int hi, k;
    cin >> hi >> k;
    for (int i = 1; (          ); i++) {
        if ( (          ) == 0) {
            cout << i << endl;
        }
    }
    return 0;
}
```

**예:** `hi = 96`, `k = 4`이면 예전과 같이 `4`, `8`, `12`, … / `hi = 15`, `k = 5`이면 `5`, `10`, `15`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `i <= hi`
- 두 번째 빈칸 → `i % k`

```cpp
#include <iostream>
using namespace std;

int main() {
    int hi, k;
    cin >> hi >> k;
    for (int i = 1; i <= hi; i++) {
        if (i % k == 0) {
            cout << i << endl;
        }
    }
    return 0;
}
```

</RevealAnswer>

---

### 문제 4 — **`0` 나올 때까지** 평균 (`while` + `break`)

`0`을 입력할 때까지 정수를 읽고, **입력했던 값들**(0 제외)의 산술평균을 출력합니다. 하나도 없으면 `0`만 출력합니다. (`while(true)` 안에서 입력 후 `0`이면 `break`; 합계는 정수 변수에 누적해도 됩니다.)

```cpp
#include <iostream>
using namespace std;

int main() {
    int sum = 0, count = 0;
    while ((          )) {
        int num;
        cin >> num;
        if (num == (          )) break;
        sum += num;
        count++;
    }
    if ((          )) cout << 0 << endl;
    else cout << (double)sum / count << endl;
    return 0;
}
```

**예:** `4 10 6 0` → 평균 약 `6.66667` / 첫 입력이 바로 `0`이면 `0`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `true`
- 두 번째 빈칸 → `0`
- 세 번째 빈칸 → `count == 0`

```cpp
#include <iostream>
using namespace std;
int main() {
    int sum = 0, count = 0;
    while(true) {
        int num;
        cin >> num;
        if(num == 0) break;
        sum += num;
        count++;
    }
    if(count == 0) cout << 0 << endl;
    else cout << (double)sum / count << endl;
    return 0;
}
```

</RevealAnswer>

---

## 조건·반복 (심화)

### 문제 5 — **세 정수 최댓값** (`if` 중첩)

정수 `a`, `b`, `c`를 입력받아 **가장 큰 값 하나**만 출력합니다. 조건문만 사용합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    int mx = a;
    if (          ) mx = b;
    if (          ) mx = c;
    cout << mx << endl;
    return 0;
}
```

**예:** `3 9 5` → `9` / `7 7 2` → `7`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `b > mx`
- 두 번째 빈칸 → `c > mx`

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    int mx = a;
    if (b > mx) mx = b;
    if (c > mx) mx = c;
    cout << mx << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 6 — **학점 판별** (`if`–`else if`–`else`)

`0 ~ 100` 점수 `s` 하나를 받아 **한 줄에 학점 문자 하나**만 출력합니다.

- 90점 이상 → `A`
- 80점 이상(90 미만) → `B`
- 70점 이상(80 미만) → `C`
- 60점 이상(70 미만) → `D`
- 나머지 → `F`

```cpp
#include <iostream>
using namespace std;

int main() {
    int s;
    cin >> s;
    if (s >= (          )) cout << 'A';
    else if (s >= (          )) cout << 'B';
    else if (s >= (          )) cout << 'C';
    else if (s >= (          )) cout << 'D';
    else cout << 'F';
    cout << endl;
    return 0;
}
```

**예:** `95`→`A`, `85`→`B`, `72`→`C`, `60`→`D`, `40`→`F`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 각 빈칸(위에서부터) → `90`, `80`, `70`, `60`

```cpp
#include <iostream>
using namespace std;

int main() {
    int s;
    cin >> s;
    if (s >= 90) cout << 'A';
    else if (s >= 80) cout << 'B';
    else if (s >= 70) cout << 'C';
    else if (s >= 60) cout << 'D';
    else cout << 'F';
    cout << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 7 — **`1 ~ m` 중 홀수만** (`for` + `%`)

정수 `m`을 입력받습니다. **`1`부터 `m`까지** 중 **홀수**만 줄마다 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int m;
    cin >> m;
    for (int i = 1; i <= (          ); i++) {
        if ( (          ) == 1 ) {
            cout << i << endl;
        }
    }
    return 0;
}
```

**예:** `m = 24`이면 `1`, `3`, …, `23` / `m = 7`이면 `1`, `3`, `5`, `7`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `m`
- 두 번째 빈칸 → `i % 2`

```cpp
#include <iostream>
using namespace std;

int main() {
    int m;
    cin >> m;
    for (int i = 1; i <= m; i++) {
        if (i % 2 == 1) {
            cout << i << endl;
        }
    }
    return 0;
}
```

</RevealAnswer>

---

### 문제 8 — **`1 ~ n` 합** (`while`)

정수 `n`(1 이상)을 받아 **`while`** 으로 `1 + … + n`을 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int s = 0;
    int i = 1;
    while ((          )) {
        s += i;
        (          );
    }
    cout << s << endl;
    return 0;
}
```

**예:** `n = 10` → `55`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `i <= n`
- 두 번째 빈칸 → `i++`

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int s = 0;
    int i = 1;
    while (i <= n) {
        s += i;
        i++;
    }
    cout << s << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 9 — **구구단 (`2`단～`9`단)** (중첩 `for`)

`2`단부터 `9`단까지, 각 줄에 `a*b=값` 형식으로 출력합니다. 한 단이 끝나면 줄을 바꿉니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    for (int a = 2; a <= (          ); a++) {
        for (int b = 1; (          ); b++) {
            cout << a << '*' << b << '=' << a * b << ' ';
        }
        cout << (          );
    }
    return 0;
}
```

**예:** 두 단 첫 줄 `2*1=2 2*2=4 …` 같은 형태(출력 간격은 맞히기만 하면 됨).

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `9`
- 두 번째 빈칸 → `b <= 9`
- 세 번째 빈칸 → `endl`

```cpp
#include <iostream>
using namespace std;

int main() {
    for (int a = 2; a <= 9; a++) {
        for (int b = 1; b <= 9; b++) {
            cout << a << '*' << b << '=' << a * b << ' ';
        }
        cout << endl;
    }
    return 0;
}
```

</RevealAnswer>

---

### 문제 10 — **사칙연산 계산기** (`switch`)

정수 두 개와 연산 문자 `op` 하나(`+` `-` `*` `/`)를 받아 결과만 출력합니다. 나눗셈은 문제에서 **항상 나누어떨어진다고** 가정합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int x, y;
    char op;
    cin >> x >> op >> y;
    switch (          ) {
        case '+':
            cout << (          );
            break;
        case '-':
            cout << x - y;
            break;
        case '*':
            cout << x * y;
            break;
        case '/':
            cout << x / y;
            break;
        default:
            cout << 0;
    }
    cout << endl;
    return 0;
}
```

**예:** `10 + 3` → `13` / `15 / 3` → `5`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `op`
- 두 번째 빈칸 → `x + y`

```cpp
#include <iostream>
using namespace std;

int main() {
    int x, y;
    char op;
    cin >> x >> op >> y;
    switch (op) {
        case '+':
            cout << x + y;
            break;
        case '-':
            cout << x - y;
            break;
        case '*':
            cout << x * y;
            break;
        case '/':
            cout << x / y;
            break;
        default:
            cout << 0;
    }
    cout << endl;
    return 0;
}
```

</RevealAnswer>

---

## 응용

### 문제 11 — **세 수 같음·둘 같음·모두 다름**

정수 `a`, `b`, `c`를 입력받아 조건에 맞는 **영문 문장 한 줄**을 출력합니다.

- 모두 같으면 `All same`
- 둘만 같으면 `Two same`
- 모두 다르면 `All different`

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    if ( (          ) ) {
        cout << "All same";
    } else if ( (          ) ) {
        cout << "Two same";
    } else {
        cout << "All different";
    }
    cout << endl;
    return 0;
}
```

**예:** `1 1 1` → `All same` / `1 2 1` → `Two same` / `1 2 3` → `All different`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `a == b && b == c`
- 두 번째 빈칸 → `a == b || b == c || a == c`

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    if (a == b && b == c) {
        cout << "All same";
    } else if (a == b || b == c || a == c) {
        cout << "Two same";
    } else {
        cout << "All different";
    }
    cout << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 12 — **문자 한 개: 알파벳 / 숫자 / 기타**

문자 하나를 입력받아 아래 규칙으로 **영단어 한 줄**을 출력합니다.

- `a~z` 또는 `A~Z` → `Alphabet`
- `0~9` → `Digit`
- 그 외 → `Other`

```cpp
#include <iostream>
using namespace std;

int main() {
    char c;
    cin >> c;
    if ((c >= 'a' && c <= 'z') || (          )) {
        cout << "Alphabet";
    } else if ( (          ) ) {
        cout << "Digit";
    } else {
        cout << "Other";
    }
    cout << endl;
    return 0;
}
```

**예:** `A` → `Alphabet` / `5` → `Digit` / `@` → `Other`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `(c >= 'A' && c <= 'Z')` 한 덩어리
- 두 번째 빈칸 → `c >= '0' && c <= '9'`

```cpp
#include <iostream>
using namespace std;

int main() {
    char c;
    cin >> c;
    if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')) {
        cout << "Alphabet";
    } else if (c >= '0' && c <= '9') {
        cout << "Digit";
    } else {
        cout << "Other";
    }
    cout << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 13 — **정수 세 개 오름차순** (배열·`sort` 금지)

`a`, `b`, `c`를 읽어 **작은 수부터** 한 줄에 공백으로 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c, t;
    cin >> a >> b >> c;
    if (a > b) {
        t = a;
        a = b;
        (          );
    }
    if (b > c) {
        (          );
        b = c;
        c = t;
    }
    if (a > b) {
        t = a;
        a = b;
        b = t;
    }
    cout << a << ' ' << b << ' ' << (          ) << endl;
    return 0;
}
```

**예:** `9 2 7` → `2 7 9`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `b = t`
- 두 번째 빈칸 → `t = b`
- 세 번째 빈칸 → `c`

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c, t;
    cin >> a >> b >> c;
    if (a > b) {
        t = a;
        a = b;
        b = t;
    }
    if (b > c) {
        t = b;
        b = c;
        c = t;
    }
    if (a > b) {
        t = a;
        a = b;
        b = t;
    }
    cout << a << ' ' << b << ' ' << c << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 14 — **로그인** (`string`)

아이디·비밀번호를 한 줄에 두 단어로 입력받습니다.

- 둘 다 맞으면 `Login success`
- 아이디만 맞고 비밀번호가 틀리면 `Wrong password`
- 아이디가 틀리면 `No such user`

```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string id, pw;
    cin >> id >> pw;
    if (id == (          )) {
        if (pw == (          )) cout << "Login success";
        else cout << "Wrong password";
    } else {
        cout << "No such user";
    }
    cout << endl;
    return 0;
}
```

**예:** `admin` `1234` → `Login success` / `admin` `9999` → `Wrong password` / `guest` `1234` → `No such user`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `"admin"`
- 두 번째 빈칸 → `"1234"`

```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string id, pw;
    cin >> id >> pw;
    if (id == "admin") {
        if (pw == "1234") cout << "Login success";
        else cout << "Wrong password";
    } else {
        cout << "No such user";
    }
    cout << endl;
    return 0;
}
```

</RevealAnswer>

---

## 추가 연습

### 문제 15 — **양수면** `Positive`

정수 `n` 하나를 받아, **양수일 때만** `Positive`를 출력합니다. `0` 이하이면 아무 것도 출력하지 않습니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    if ( (          ) ) {
        cout << "Positive" << endl;
    }
    return 0;
}
```

**예:** `5` → `Positive` / `-1` → (출력 없음)

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `n > 0`

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    if (n > 0) {
        cout << "Positive" << endl;
    }
    return 0;
}
```

</RevealAnswer>

---

### 문제 16 — **짝수 / 홀수** (`Even` / `Odd`)

`n`을 입력받아 짝수면 `Even`, 홀수면 `Odd`를 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    if ( (          ) == 0 ) {
        cout << "Even";
    } else {
        cout << "Odd";
    }
    cout << endl;
    return 0;
}
```

**예:** `8` → `Even` / `7` → `Odd`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `n % 2`

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    if (n % 2 == 0) {
        cout << "Even";
    } else {
        cout << "Odd";
    }
    cout << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 17 — **학점 (4단계)** `A` `B` `C` `F`

`0~100` 점수에서 **85 이상 A**, **75 이상 B**, **65 이상 C**, 나머지 **F**만 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int s;
    cin >> s;
    if (s >= (          )) cout << 'A';
    else if (s >= (          )) cout << 'B';
    else if (s >= (          )) cout << 'C';
    else cout << 'F';
    cout << endl;
    return 0;
}
```

**예:** `90` → `A`, `74` → `C`, `50` → `F`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 순서대로 → `85`, `75`, `65`

```cpp
#include <iostream>
using namespace std;

int main() {
    int s;
    cin >> s;
    if (s >= 85) cout << 'A';
    else if (s >= 75) cout << 'B';
    else if (s >= 65) cout << 'C';
    else cout << 'F';
    cout << endl;
    return 0;
}
```

</RevealAnswer>

---

### 문제 18 — **둘 다 짝수 / 둘 다 홀수 / 하나만 짝수**

`cout`으로 입력 안내를 찍은 뒤 정수 `a`, `b`를 받습니다. **둘 다 짝수**면 `Both even`, **둘 다 홀수**면 `Both odd`, **그 외**(짝·홀 한쪽만)면 `One even`을 출력합니다.

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b;
    cout << "첫 번째 정수: ";
    cin >> a;
    cout << "두 번째 정수: ";
    cin >> b;

    if ((          )) {
        cout << "Both even" << endl;
    } else if ((          )) {
        cout << "Both odd" << endl;
    } else {
        cout << "One even" << endl;
    }
    return 0;
}
```

**예:** 첫 수 `4`, 둘째 `10` → `Both even` / `4`, `3` → `One even` / `1`, `9` → `Both odd`

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `a % 2 == 0 && b % 2 == 0`
- 두 번째 빈칸 → `a % 2 == 1 && b % 2 == 1`

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b;
    cout << "첫 번째 정수: ";
    cin >> a;
    cout << "두 번째 정수: ";
    cin >> b;

    if (a % 2 == 0 && b % 2 == 0) {
        cout << "Both even" << endl;
    } else if (a % 2 == 1 && b % 2 == 1) {
        cout << "Both odd" << endl;
    } else {
        cout << "One even" << endl;
    }
    return 0;
}
```

</RevealAnswer>

---

### 문제 19 — **세 수 최댓값 (함수 금지)**

`max()`, 정렬 라이브러리 없이 `if`만으로 세 정수 중 최댓값 출력(문제 5와 같은 로직 직접 기입 연습용).

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    int mx = a;
    if (          ) mx = b;
    if (          ) mx = c;
    cout << mx << endl;
    return 0;
}
```

**예:** 문제 5와 동일.

<RevealAnswer title="정답 보기 (클릭하여 펼침)">

- 첫 번째 빈칸 → `b > mx`
- 두 번째 빈칸 → `c > mx`

```cpp
#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cout << "정수 1: ";
    cin >> a;
    cout << "정수 2: ";
    cin >> b;
    cout << "정수 3: ";
    cin >> c;

    if (a >= b && a >= c) {
        cout << a << endl;
    } else if (b >= a && b >= c) {
        cout << b << endl;
    } else {
        cout << c << endl;
    }
    return 0;
}
```

</RevealAnswer>

---
